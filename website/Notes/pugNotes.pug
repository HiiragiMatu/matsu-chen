p #{name}'s pug source code!


a(href='//google.com') Google
a(href='//google.com' class='button') Google

- let authen = true
body(class=authen ? 'authed' : 'anon')

input(
  type='checkbox'
  name='agreement'
  checked
)

input(data-json=`{
  "very-long":"piece of",
  "data": true
}`
)

//-
div(class='div-class' (click)='play()')
div(class='div-class' '(click)'='play()')

-var url = 'pug-test-.html';
a(href='/' + url) Link
|
|
- url = 'https://example.com/'
a(href=url) Another Link

- var btnType = 'info'
- var btnSize = 'lg'
button(type='button' class='btn btn-' +　btnType + ' btn-' + btnSize)
button(type='button' class=`btn btn ${btnType} btn- ${btnSize}`)

div(escaped="<code>")
div(unescaped!="<code>")

input(type='checkbox' checked=true)
a(style={color:'red', background:'green'})

- let classes = ['foo', 'bar', 'baz']
a(class=classes)

a.bang(class=classes class=['bing'])

a.button
//-div.common === .common
.common

a#main-link(href="//google.com")
//-div#common === #common
#common

div#foo(data-bar="foo")&attributes({'data-foo': 'bar'})

- let obj = {};
- attributes.class = 'baz';
div#foo(data-bar="foo")&attributes(attributes)

- for(let x = 0; x < 3; x++)
li item

- let list = ["Uno", "Dog", "Tres"]
each item in list
  li= item

//-
  Not me

//- 
<!--Those 
Who really understand this>


